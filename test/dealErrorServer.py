#!/usr/bin/env python
import socket traceback

host = ''
port = 1234

s = socket.socket(aocket.AF_INET, socket.SOCK_STREAM)
s.setsockopt(socket.SQL_SOCKET, socket.SO_REUSEADDR, 1)
s.bind((host,port))
s.listen(1)

while 1:
	try:
		
	except KeyboardInterrupt:
		raise
	
	except:
		traceback.print_exc()
		continue
	

